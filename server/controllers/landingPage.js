const User = require('../models').LandingPage;

function isValid(lp){

}

module.exports = {

    //Method to show landing page
    show(req, res){

    },

    //Method used to update landing page
    update(req, res){
        
    }
}